import { EnableEditDirective } from './enable-edit.directive';

describe('EnableEditDirective', () => {
  it('should create an instance', () => {
    const directive = new EnableEditDirective();
    expect(directive).toBeTruthy();
  });
});
